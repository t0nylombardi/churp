<%= render 'shared/left_side', tweet: @tweet %>

<!-- Middle -->
<div class="w-full sm:w-600 h-screen scrollbar-hide">
  
  <%= render 'shared/header', view: :show %>

  
  <%= render 'show_tweet', tweet: @tweet %>

</div>
<!-- /Middle -->

<%= render 'shared/right_side' %>