<%= turbo_frame_tag(
      "tweets-page-#{@pagy.next}",
      # autoscroll: true,
      loading: :lazy,
      src: pagy_url_for(@pagy, @pagy.next),
      target: "_top"
    ) do %>
  <%= render 'tweets/shared/spinner' %>
<% end %>